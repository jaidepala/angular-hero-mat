import { ReplaceWithHyphenPipe } from './replace-with-hyphen.pipe';

describe('ReplaceWithHyphenPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceWithHyphenPipe();
    expect(pipe).toBeTruthy();
  });
});
